record_symbols: [BTCUSDT, ETHUSDT, BNBUSDT]

log_level: DEBUG

db:
  host: localhost
  port: 3306
  user: root
  passwd: root
  db_name: binance_harvester
  commit_freq: 50  # How often database transactions are committed.